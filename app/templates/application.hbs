<section class={{this.themeClass}}>
    <Header::Header @onThemeSwitcherClick={{this.onThemeSwitcherClick}} />

    <Page::Page @bestPlayers={{model.bestPlayers}} @onBestPlayerClick={{this.onBestPlayerClick}}>
        {{outlet}}
    </Page::Page>
</section>
